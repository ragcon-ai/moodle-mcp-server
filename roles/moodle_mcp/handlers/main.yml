---
- name: Restart moodle-mcp-server
  ansible.builtin.systemd:
    name: "{{ mcp_service_name }}"
    state: restarted
    daemon_reload: true

- name: Reload nginx
  ansible.builtin.systemd:
    name: nginx
    state: reloaded
